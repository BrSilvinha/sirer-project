# Variables de entorno para el backend de SIRER

# Configuración de la base de datos
DATABASE_URL=postgresql://username:password@host:port/database_name

# JWT Secret (usar un string aleatorio seguro)
JWT_SECRET=tu_jwt_secret_super_seguro_aqui

# URL del frontend (para CORS)
FRONTEND_URL=http://localhost:3000

# Entorno de ejecución
NODE_ENV=development

# Puerto del servidor (Render usa PORT automáticamente)
PORT=5000

# Ejemplo para Render PostgreSQL:
# DATABASE_URL=postgresql://sirer_database_user:password@dpg-xyz123.oregon-postgres.render.com/sirer_database

# Ejemplo para producción:
# FRONTEND_URL=https://tu-frontend.onrender.com
# NODE_ENV=production